# Handoff — 2026-07-17 (night), written at 42e3788

Successor: read this + MEMORY.md first. The repo is the source of truth;
every claim below has a plan doc or ledger entry behind it.

---

## Addendum — 2026-07-18 (a5e9323, 4d166a8)

**A5 driver authored and plumbed.** `--kl-pair MODEL_B --kl-pair-out FILE`
on `q27-metal` (plan: `docs/plans/2026-07-18-kl-pair-a5.md`). Two
independent `MetalEngine::Shared` mappings, fp16-KV both sides, 12-token
lockstep teacher forcing with `q27::forward_kl` on CPU in double — the
`--kl-kv` machinery lifted across models. Plumbing gates both directions
pass: `--kl-kv-self` T2 vs T2 512 pos = 0.0 exactly; `--kl-pair` T2 vs B1
8191 pos = 0.389 nats mean (p99 2.45, max 8.44 @pos 5573), >0 at every
position. M1 vs B1 sanity: 99 pos in 6.86 s = 14.4 pos/s, mean KL 0.027
nats (M1 is B1 + 42 MB of T2 grafts, so small KL is the expected sign).
test-cpu ALL PASS incl. test_kl.

**Mini merged at 4d166a8.** Mini shipped the mixed-tier milestone at
`64e6766` (M1 = `cheap_pair` = B1 + T2{gdn_alphabeta, attnq_mid}, 3.83 GB,
passes the full pre-registered ship gate). Three commits pulled via the
`mini` remote; conflicts resolved by taking mini's side on README.md and
the census plan doc (mini had the results; our side was still pending).
The M1 artifact rsynced to `models/bonsai-27b-m1/` on this M4 (md5
91db7fdd…, engine-validates in 0.99 s).

**A5 measurement is BLOCKED on hardware, not on the driver.** The
M4 (24 GiB) cannot hold M1 (3.83 GiB) + official (17.73 GiB) = 21.56 GiB
simultaneously: the driver reaches "ready" then the first
`teacher_force_logits` chunk hangs in `waitUntilCompleted` with the GPU
thrashing (wired pages peak 861 K → 638 K, log frozen at header). The
same binary runs M1+B1 (7.6 GiB) and T2+B1 (10.9 GiB) cleanly.
**A5 moves to yukon** — the operator will port `--kl-pair` to the CUDA
engine. The M1 pack and the code corpus
(`data/codeparrot-python-2mb.tokens.bin`, 524,779 tokens from
codeparrot/codeparrot-clean Python, 276 files / 270 repos) are staged
on this M4 ready to rsync. Ship line is unchanged: KL(M1‖official) <
KL(T2‖official) on BOTH wikitext AND the code corpus, paired-bootstrap
95% CI on KL(M1)−KL(T2) excluding 0 from below.

**Mini is mid-flight on more work** (358 files / +41,717 lines staged,
uncommitted): metal_server.cpp +978, api_common.h +330,
tools/eval/ (8 new files, the capability spot-check machinery),
tools/agentic_replay_*.py (3 new files, the replay bench). Wait for it
to commit+push before running autoreview on the full delta.

---

## Addendum 2 — 2026-07-18 (night): A5 RAN, verdict KILL on both corpora

The yukon port was cancelled in favor of a sequential two-pass design on
the M4 (operator-approved): `--logits-dump` streams a baseline model's
teacher-forced f32 logits to disk (17 GiB official runs alone, fits);
`--kl-vs-dump` replays the dump against each candidate alone (mmap,
chunk-wise). Mathematically identical to lockstep — G3' proved it: M1-vs-
B1-via-dump reproduces the lockstep `--kl-pair` numbers to the printed
digits (mean 0.0266966, max 0.112171 @pos 78). G2' self-vs-own-dump = 0.0
exactly. Both modes landed in `metal_cli.cpp`; plan doc carries the full
amendment + results table.

**A5 verdict (4K positions, both corpora, paired block bootstrap):**

| corpus | KL(T2‖off) | KL(M1‖off) | M1−T2 [95% CI] | verdict |
|---|---|---|---|---|
| wikitext | 0.7499 | 0.7956 | +0.0458 [+0.018, +0.075] | **KILL** |
| code | 0.4766 | 0.6519 | +0.1753 [+0.151, +0.203] | **KILL** |

M1's 48%-of-gap NLL recovery is the endpoint-overshoot artifact review 2
flagged: on wikitext the graft's distributional effect vs B1 is
indistinguishable from zero (+0.002 [−0.008, +0.013]); on code it is a
small regression (+0.020 [+0.011, +0.030]). The conjunctive-gate design
(NLL alone never shipped anything) worked as intended. **The mixed-pack
serving thesis closes; the two-tier ladder (B1 small, T2 quality)
stands.** A6/product gates are moot for M1. `gdn_pair` rescue residue
stays parked. Evidence: `logs/kl-pair-20260718/` (6 per-position .kl
files + 2 official dumps, retained for future candidates).

**Open work re-ranked after the A5 kill:** (1) watch pi traffic for the
unreproduced "not a valid tool call" report; (2) registered residues
(sanitizer stepper refactor, test_tokenizer in test-cpu, /v1/messages +
/v1/responses streaming parity, CUDA adoption of ToolCallStreamer);
(3) 32K ternary NLL (gate 3) overnight candidate; (4) mini's in-flight
eval/replay work — merge when it lands; (5) gdn_pair rescue stays
parked unless separately funded. Homebrew: still deferred by the
operator.

## Live state (right now)

- **T2 server is RESIDENT and serving real pi traffic** on :8213, binary
  built at 42e3788. Canonical line (also in tools/quiet_q4_bench.sh
  `start_server`): `nohup caffeinate -i nice ./build/q27-metal-server
  models/ternary-bonsai-27b/ternary-bonsai-27b-t2.q27
  models/qwen36-27b-mtp/qwen36-27b-mtp.tok --ctx 131072 --port 8213
  --suffix 32 --snapshot-dir ~/.q27/snapshots --max-tokens-default 16384
  --trace ~/.q27/trace.jsonl`
- **Never restart it during a live turn**: quiet-gap protocol = trace
  idle ≥45 s AND last event kind outcome/cancel/error. pid match must be
  argv[0] `./build/q27-metal-server` exactly (killing the caffeinate
  wrapper orphans a 17 GB process — caused the OOM crashes).
- **GPU-exclusive slot rule (spans sessions, in MEMORY.md):** one
  model-loading q27 binary at a time on this 24 GiB M4. Agents
  edit/compile only. After any crash or if another agent may be active,
  ask the operator who owns the slot — pgrep is a race that crashed the Mac
  twice.
- The mac-mini is a SEPARATE machine running its own queue (below);
  it pushes to origin/metal — fetch before you push.

## What shipped today (all pushed, all gated)

1. **Q4 half GEMM remains default-off; quiet verdict pending.** The 0.855×
   run was forced with `idle>=0s` and is rejected as quiet-gate evidence
   (`logs/q4port-20260717/QUIET_BENCH_EVIDENCE.md`). Probe knob
   `Q27_METAL_GEMM_HALF_Q4=1`; T2 keeps its shipped `_h` default.
2. **B1 4-row GEMV promoted** (round 2, plan:
   2026-07-17-b1-select-round2.md). Key correction recorded: decode is
   at the memory wall; the 2.36× bench win didn't move artifact decode —
   ships because strictly dominating. B1's next 15% lives in the fixed
   ~14 ms non-GEMV cost, NOT the kernel (two independent reviews
   converged on this — dilution is now the primary model).
3. **Serving-stack hardening from live pi bugs**: drift mode 10
   (in-string quotes), stream keepalives on all three APIs, and
   **incremental tool-call argument streaming** on chat
   (plan: 2026-07-17-incremental-tool-call-streaming.md — read its
   Review round section, it's the freshest state).
4. **Autoreview round (42e3788)**: 4 confirmed correctness defects
   fixed same-night, each proven both ways with sabotage builds:
   packed-wrapper second-call loss, keepalive blind spots in
   queue-wait/prefill (live() probe now fires keepalives, runs
   UNLOCKED in the queue wait), inline mode-3 repair in the streamer,
   mode-10 lookahead restricted to balanced segments.
5. **Eval machinery** (cb099ee): 120-prompt capability spot-check +
   verdict bands; **agentic replay bench** (9649f2b): trace→corpus→
   replay, corpus accumulating from real traffic (too thin to publish;
   first real run needs a representative week + a quiet slot).
6. **Two expert-review triages adopted** (plans:
   2026-07-17-expert-review-triage.md, 2026-07-18-expert-review-2-
   triage.md). Census plan now carries the A0–A7 combination-arm
   sequence; QA checklist gained the feature-interaction matrix +
   thermal governance; suffix plan gained the sampled-path battery
   pre-registration.

## The mini's queue (don't duplicate)

The mini subsequently published the full 25-arm census, ran three combo
arms, and ran the original suffix/probe gate. `gdn_pair` (~4.11 GB) wins
NLL but FAILS the constraints probe (3/4) and is experimental only.
`cheap_pair` (3.83 GB) passes the original NLL/suffix/probe gate and is the
provisional M1 candidate, not a serving tier. Remaining queue: A1 paired
bootstrap/holdout addendum → A5 cross-model KL → author + must-fail-test
the amended A6 ground-truth non-inferiority verdict → run provenance-
checked `t2-base`, `b1-base`, and `m1-candidate` (the non-decision gdn-qkv
arm is dropped) → direct decode, two-slot, and 131K pressure gates. Do NOT treat `spotcheck_verdict.py` as
A6; it reports supporting agreement only.

## Open work, in rough priority

- **A5 KL(mixed‖official) driver needs authoring** for the provisional
  `cheap_pair`/M1 candidate — the decisive quality gate; --kl-kv is a
  same-model comparator, this is cross-model paired-logit (tokenizer
  byte-identical; ~a session). The original census logs publish scalar
  NLLs, not per-position losses, so A1's paired bootstrap needs a rerun or
  an instrument extension rather than an invented interval.
- **Watch pi traffic**: one unreproduced "not a valid tool call" report
  post-streaming-deploy. If it recurs, pull the exact request from
  ~/.q27/trace.jsonl (ids reset per boot — scope by boot event + tms).
- Registered residues: /v1/messages input_json_delta + /v1/responses
  argument-delta streaming; CUDA server adoption of ToolCallStreamer +
  keepalives + cancel patch at next yukon merge; sanitizer state
  machine exists in 3 places/2 idioms (shared stepper refactor);
  test_tokenizer is NOT in test-cpu (vacuous-pass trap — run it
  directly with the .tok file); /v1/completions deliberately has NO
  keepalive (replay-bench TTFT reads first byte).
- 32K ternary NLL (gate 3) remains an overnight candidate; constraint-
  decode cost profile staples to the next warm slot.
- **Homebrew: deferred by the operator until "done done".** Don't start it.

## House rules that bit us (obey them)

Pre-register ship/kill lines before measuring. Check exit codes, never
grep for PASS. Gates must fail both ways (sabotage). Ceiling and
artifact numbers only compare back-to-back at matched thermal state.
Attribution in docs is "the operator", never the personal name. The
repo is PUBLIC — no unlicensed vendor files (bonsai-27b-whitepaper.pdf
stays local, gitignored).
