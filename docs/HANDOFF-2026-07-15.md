# Session handoff — 2026-07-15 (delete after pickup)

**UPDATE (final sync): the attribution pair is DONE and its verdict is in
the ledger** (hypothesis B — machine state; causal GQA −20.1% wall at 16K
at quality parity; `caffeinate -dims` every long run). The "running at
handoff" and "question the pair answers" sections below are now history.
**Repo ownership: this mac-mini session owns the repo from here.** The
24 GB machine's session will no longer rsync `.git` (it would clobber your
commits); it pulls instead. Current head at final sync: see `git log`.
Remaining pickup tasks, in order: ~~sampling stage 2~~ (landed 2026-07-15,
see METAL_PROGRESS stage-2 entry — byte-identical A/B, 7 configs),
~~whitepaper item 1 KL KV gate~~ (landed 2026-07-15 with first 8K
measurement: 0.0115 nats mean, depth-flat, heavy-tailed — see ledger),
whitepaper follow-ups items 2 and 6 (native ternary packing, llama.cpp MTP
claim verification) in the ternary plan doc. Full-Metal codex sweep
(gpt-5.6-sol) in flight 2026-07-15 afternoon; note codex on this machine
now uses API-key auth (ChatGPT auth stale, backed up).

For a fresh Claude Code session on the mac-mini (`~/projects/q27`, user
`minime`) continuing the Phase 4 / post-ternary work. Read
`docs/METAL_PROGRESS.md` (last four entries) and
`docs/plans/2026-07-14-ternary-tier.md` (Phase 4 sections) first; this file
covers only what is in flight and not yet in the ledger.

## State at handoff

- Branch `metal` at `9ac086a`. All gates green: full `make test-metal`
  (incl. GQA parity + straddle gates), gate 3 PASSED (PPL 11.693,
  depth-uniform ~2.2x vs official — see ledger). Codex findings 1 and 3
  fixed, finding 2 fixed in `9ac086a` (per-tensor logical bounds).
- This mac-mini is a certified second rig: suite green, T2 artifact
  md5-verified (`8babb56d...`), `maxBufferLength` 8.0 GiB so the artifact
  takes the whole-mapping + residency-set path (first 16 GiB machine to).
  Official 17 GiB artifact does NOT fit here — official-tier runs stay on
  the 24 GB machine.
- Memory-safe policy applies here too, tighter: 16 GiB total, 6.66 GiB
  wired at load. One model load at a time, ever.

## Running at handoff (launched ~11:50, caffeinated, nohup)

16K NLL attribution pair in `~/projects/q27/`, sequential:
1. `nll16k_old.log`  — `Q27_METAL_GQA_THRESHOLD=0` (legacy kernels)
2. `nll16k_gqa.log`  — default threshold 2048 (causal GQA at depth)

Command per run: `./build/q27-metal models/ternary-bonsai-27b/...-t2.q27
models/qwen36-27b-mtp/qwen36-27b-mtp.tok --nll data/wikitext2-test.tokens.bin
--nll-long 16384 --ctx 16384 --kv turbo3`

## The question the pair answers

Gate 3 on the 24 GB machine ran at 0.99 tok/s overall vs the official
artifact's 4.39 tok/s for the same 32K pass — 4.4x slower despite streaming
fewer weight bytes, with the collapse concentrated after ~position 11k.
Hypotheses: (A) T2-specific code path (float-activation chunk GEMM route) —
would REPRODUCE here; (B) the 24 GB machine's overnight power state — would
NOT reproduce here (this pair is caffeinated on an idle machine).

Reference points: the 24 GB machine did the same 8K pass in 751.1 s
(old kernels) / 692.7 s (GQA). Its gate-3 pace through 16K was ~2.5-3 h.
- Pair finishes ~30-45 min/run and buckets match gate 3 (0-2k 9.483,
  2k-8k 15.409): hypothesis B — record that overnight long runs need
  `caffeinate`, close the anomaly, rerun nothing.
- Pair reproduces the collapse: hypothesis A — next step is
  `Q27_METAL_PROFILE=1` on an 8K slice here to attribute the kernel share
  (expect `q27_matmul_t2_mm` or the fp32-activation route to dominate).
- GQA run vs old run: the delta at 16K extends the -7.8%-at-8K curve;
  record it in the ledger with both walls.

## GPU-assisted sampling — stage 1 landed, stage 2 is the pickup task

`a3e4722` adds `q27_topk_logits` (16-bit radix select, one threadgroup,
capacity-1024 candidate compaction) + `ComputeBackend::topk` + gates. The
sampled decode path still reads back the full 600 KB logits per token
(`generate_sampled_from_logits`, metal_engine.cpp ~line 921).

Stage 2 (not started, cleanly scoped):
1. `sampling.h`: add `sample_candidates_cpu(values, indices, count, params,
   rng)` — the existing post-sort logic operating on a candidate list; one
   uniform draw, same RNG consumption as `sample_logits_cpu`.
2. Engine: when `params.top_k` in [1, 256], run `backend_.topk` on
   `logits_` (k = top_k), read ~count pairs, sort desc (value, then index
   asc), truncate to k, sample. `count > capacity` or `top_k` outside
   [1,256] -> existing full-readback path. CPU top-k order on exact float
   ties is unspecified (`std::partial_sort`); the GPU path's index-asc tie
   order is a superset-deterministic refinement — note it in the ledger.
3. Gate: same-seed A/B old-vs-new over several seeds/temperatures on real
   artifact logits (`--temperature 0.8 --top-k 40 --top-p 0.95 --seed N`)
   — token sequences must match exactly (real logits don't tie).

## Open threads after the pair

1. Ledger entry for the pair's verdict (METAL_PROGRESS + plan doc
   follow-up section) — commit from this machine is fine; push to
   `origin/metal` needs the 24 GB machine's key (or add this machine's).
2. Short caffeinated confirmation slice on the 24 GB machine (only if
   hypothesis B needs same-machine confirmation).
3. Next roadmap levers, choose with the attribution data: long-context
   cache-block scheduling vs chunk-GEMM staging (leverage order in
   METAL_PROGRESS critical-path section).
4. Suffix-drafting effectiveness on the ternary artifact (plan doc lists it
   as an open Phase 4 question).

## Machine notes

- 24 GB machine: user's daily driver — ask before GPU runs there.
- Do not re-run gate 3 (closed, PASS). Do not load two models anywhere.
- `build/q27-metal-base` / `-res` on the 24 GB machine are preserved
  pre/post-residency binaries for A/B; not synced here.
