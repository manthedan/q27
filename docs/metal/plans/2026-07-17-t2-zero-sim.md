# T2 zero-aware repack Phase-0 — CPU sim (2026-07-17): KILL

k3 roadmap item #3, adopted d5a5674 with the pre-registered kill line
"net byte saving < 8% -> stop before writing a kernel". Probe:
tools/t2_zero_sim.cpp, a CPU mmap scan of the production T2 pack
counting zeros and all-zero 128-column groups (the only stream-compatible
skip unit — the T3 autopsy rules out per-element indirection).

## RESULTS (logs/k3-fixes-20260717/t2-zero-sim.out)

**KILL: net saving −0.37%** (bitmap overhead exceeds the saving, because
the saving is zero). Sanity: overall zero fraction 29.70% — matches the
recorded headroom number exactly, so the layout read is correct.

The explanatory histogram: of 210M groups, **0 are all-zero**; 93.84%
hold 32–63 zeros and 6.16% hold 1–31 — the zeros sit at ~30% density in
essentially every group, i.e. uniform scatter, not structure. Per-class
identical (attn 30.68% / ffn 29.21% / gdn 30.75% / other 30.06%, all
0.00% group-contiguous). Code-3 integrity scan: clean.

Closure: "29.7% zeros" leaves the headroom ledger — it is entropy-bearing
scatter the ternary code already prices, not skippable bytes. Any future
zero-exploiting idea must beat this histogram, not the headline fraction.
