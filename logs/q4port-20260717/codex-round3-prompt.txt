Review the uncommitted changes in src/metal/metal_server.cpp (git diff; ignore q27_kernels.metal/metal_backend.mm/README — separate reviewed rounds). This is FIX-VERIFICATION round 3 on the /v1/responses port + EngineError split. Round 2 accepted the THINK-transition and response.failed fixes but found one P2: non-stream bare-call recovery only scanned the final uncommitted text buffer, so a COMPLETE bare call committed at a think/tool transition flush was never recovered.

The round-3 fix: non-stream flush_text now runs parse_bare_tool_calls on EVERY text segment before committing it (tx=pre convention; pre-call prose message precedes the recovered function_call/custom_tool_call items in model output order; push_call was reordered above flush_text). The end-of-turn recovery block is gone — the final segment rides the same flush path. Streaming path unchanged (CUDA parity: deltas are cosmetic; end-of-turn text_accum recovery still emits the call items).

Verify with file:line: (1) no segment can now commit an unrecovered complete bare call as message text in the non-stream path; (2) definition order captures are valid (flush_text references push_call/items/tool_counter/custom_names/rn — all in scope at definition); (3) no double-recovery or double-message on any turn shape (prose only; bare call only; prose+call; call+think; think+call; call1+call2 in one segment; wrapped call then bare call); (4) the malformed-wrapper path (flush_tool !c.ok pushes c.raw as message text WITHOUT recovery) — confirm acceptable or flag if a nested bare call there is a real coverage hole vs the chat/Anthropic handlers' convention (they append c.raw to tx and then recover over the combined text); (5) per-segment parse_bare_tool_calls cannot false-positive worse than the previous end-of-turn scan; (6) drift-catalog logging behavior change is log-only. P1/P2/P3 verdicts.
