Review the uncommitted changes in src/metal/metal_server.cpp AND src/api_common.h (git diff; ignore q27_kernels.metal/metal_backend.mm/README). FIX-VERIFICATION round 5 on the /v1/responses port. Round 4 accepted the repair-gating but found one P2: flush_tool folding malformed raw into the shared text buffer meant a later recovery's tx=pre could drop prose that followed the wrapper.

The round-5 fix: flush_tool(bool final_turn) now commits malformed raw as its OWN segment immediately (text+=c.raw; flush_text(final_turn)) — recovery still runs over it (round-3 hole stays closed), but later prose accumulates in separate segments where tx=pre cannot reach it. route calls flush_tool(false) at transitions; the end-of-turn call passes true so a max_tokens-truncated wrapper still gets the repair path. The remaining tx=pre boundary (prose after a recovered call within one NATURAL segment) is documented as the house convention, identical in the chat/Anthropic handlers.

Verify with file:line: (1) no path from model output to committed items can now drop text that followed a malformed wrapper; (2) item order is model-order for: wrapped-call → text, wrapped-call → think, malformed → malformed, malformed(at max_tokens truncation, end-of-turn) → repair rescue; (3) the fold cannot double-flush (end-of-turn sequence flush_tool(true) then flush_text(true)); (4) definition order (flush_tool references flush_text — both defined before route); (5) the natural-segment tx=pre coarseness is genuinely identical to the chat/Anthropic convention (check lines ~1371 and ~1552). P1/P2/P3 verdicts.
