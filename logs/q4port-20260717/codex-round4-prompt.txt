Review the uncommitted changes in src/metal/metal_server.cpp AND src/api_common.h (git diff; ignore q27_kernels.metal/metal_backend.mm/README — separate reviewed rounds). FIX-VERIFICATION round 4 on the /v1/responses port. Round 3 found two P2s, both addressed:

1. (round-3 P2) Per-segment recovery ran the FULL parser including truncation repair at mid-turn segment boundaries. Fix: parse_bare_tool_calls gained a defaulted 4th param `allow_trunc_repair` (api_common.h; default true = every pre-existing caller unchanged); the unbalanced-{"name" repair branch is skipped when false. Non-stream flush_text is now flush_text(bool final_turn): transition flushes pass false, the end-of-turn flush passes true. Verify: every non-stream flush_text call site passes the right flag; repair still rescues a genuine max_tokens-truncated final call; the streaming path and chat/Anthropic handlers are behaviorally unchanged.

2. (round-3 P2) The malformed wrapped-call path committed c.raw as a message with no recovery (chat/Anthropic fold c.raw into tx first). Fix: non-stream flush_tool now folds c.raw into the pending text buffer (text+=(text.empty()?"":"\n")+c.raw) so the next text commit runs recovery over it. Verify: item ORDER stays model-order for every interleaving (malformed wrapper then text; then think; then another wrapped call; malformed wrapper at end-of-turn); no double-recovery; no path commits text without recovery now EXCEPT the streaming path (CUDA parity, cosmetic-delta contract — out of scope).

Also confirm the api_common.h change cannot alter behavior for any existing caller (default arg) and the repair-gate condition placement (inside the end==npos branch) is exact. P1/P2/P3 verdicts with file:line.
